let Koa,Router,bodyParser,mongoose,api;_87a‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_87a‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_87a‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_87a‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_87a‍.w("./api",[["default",["api"],function(v){api=v}]]);require('dotenv').config();







const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false })
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _87a‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes());

app.use(bodyParser());
// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(4000, () => {
  _87a‍.g.console.log('Listening to port %d', port);
});
