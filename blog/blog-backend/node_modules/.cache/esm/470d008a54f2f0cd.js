let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_54f‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_54f‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_54f‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_54f‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_54f‍.w("./api",[["default",["api"],function(v){api=v}]]);_54f‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false })
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _54f‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes());

app.use(bodyParser());
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(4000, () => {
  _54f‍.g.console.log('Listening to port %d', port);
});
